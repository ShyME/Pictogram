/**
 * A viewer's home view, assembled on read (ADR-0003) from the {@code follow} and {@code post} published interfaces.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Feed")
package me.imshy.pictogram.feed;
