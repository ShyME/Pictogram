/**
 * Holds uploaded images as bytes, re-encoded server-side to one canonical square format (an Original plus a Thumbnail), EXIF stripped.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Media")
package me.imshy.pictogram.media;
