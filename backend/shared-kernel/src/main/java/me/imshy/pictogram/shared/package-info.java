/**
 * ID value types every bounded context may depend on. Spring Modulith module {@code shared},
 * whitelisted via {@code @Modulithic(sharedModules = "shared")}; Gradle subproject
 * {@code :shared-kernel}. Nothing but IDs belongs here — no entities, no DTOs, no behaviour.
 */
package me.imshy.pictogram.shared;
