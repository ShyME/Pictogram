/**
 * The public face of a user — username, display name, bio — and the onboarding step that creates it. Emits {@code ProfileUpdated}.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Profile")
package me.imshy.pictogram.profile;
